<?xml version="1.0" encoding="UTF-8"?>
<Algorithm_Parameters>

    <Algorithm Type="Basic_Scheme">                     <!-- "Default" (Basic_Scheme) ou "Basic_Scheme" -->
        <Convergence_Criterion Value="Default"/>        <!-- "Default" (1e-4) ou valeur réelle positive <1e-3 -->
        <Convergence_Acceleration Value="true"/>        <!-- "True" ou "False" -->
    </Algorithm>

   <Mechanics>
       <Filter Type="Default"/>                                         <!-- "Default" (hexa) ou "no_filter" ou "hexa" ou "octa" -->
       <Small_Perturbations Value="true" Displacement_Gradient="nsym"/> <!-- Value = "True" ou "False" / gradU = "sym" or "nsym", default is "sym" --> 
       <C0sym Value="false"/>   
   </Mechanics>

   <Non_local_algorithm NLocMod_num="1"  Algo="explicit">
       <P_real Index="1" Value="2" />                     <!-- 1, 2 or 3 : chose discrete differential operators --> 
   </Non_local_algorithm>                                         <!-- 1 : rot = DF centree     rotrot = operateur 'rotrot'-->
                                                                  <!-- 2 : rot = DF centree     rotrot = rot(DF centree) applique deux fois-->
                                                                  <!-- 3 : rot = DF de la meca (hexa par defaut)     rotrot = rot (DF de la meca) applique deux fois-->

</Algorithm_Parameters>
